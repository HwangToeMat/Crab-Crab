version: '3.8'

services:
  crab-infinity:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ../../services:/app/services:ro
      - ../../config:/app/config:ro
    environment:
      - ENV=production
    restart: always
