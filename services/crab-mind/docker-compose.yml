version: '3.8'

services:
  crab-mind:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY:-MOCK_KEY}
    volumes:
      - ./backend:/app/backend
      - ./frontend:/app/frontend
